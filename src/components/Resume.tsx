import '@/styles/resume.css';

export const Resume = () => {
  return (
      <section id="resume" className="resume-section">
          <h2>Resumé:</h2>
          <button>Download Resumé</button>
      </section>
  )
}
